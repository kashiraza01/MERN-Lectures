/**
 * Task 6 — Shared module (complete after Task 4)
 *
 * Export a function: formatLogEntry({ level, message, timestamp })
 * Returns: "[LEVEL] timestamp message"
 */

// TODO: module.exports = function formatLogEntry(entry) { ... }
